{
  "name": "picaso-token",
  "version": "2.0.0",
  "description": "Collateralized-NFT demo: deposit an ERC20 to mint a PicasoToken NFT, burn it to redeem through Bancor.",
  "type": "module",
  "license": "ISC",
  "workspaces": [
    "frontend"
  ],
  "author": "emilbob",
  "keywords": [
    "solidity",
    "erc721",
    "erc20",
    "bancor",
    "hardhat"
  ],
  "scripts": {
    "build": "npm run build:contracts && npm run gen && npm run build:frontend",
    "build:contracts": "hardhat clean && hardhat compile",
    "build:frontend": "npm --workspace frontend run build",
    "gen": "node scripts/export-abi.mjs",
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "dev": "npm --workspace frontend run dev",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy-demo.ts --network localhost",
    "test": "hardhat test",
    "coverage": "hardhat test --coverage",
    "gas": "hardhat test --gas-stats",
    "lint": "solhint \"contracts/**/*.sol\"",
    "lint:fix": "solhint \"contracts/**/*.sol\" --fix",
    "check:deployment": "node scripts/check-deployment.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.0",
    "@nomicfoundation/hardhat-ethers-chai-matchers": "^3.0.0",
    "@nomicfoundation/hardhat-ignition": "^3.0.0",
    "@nomicfoundation/hardhat-ignition-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-keystore": "^3.0.0",
    "@nomicfoundation/hardhat-mocha": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@nomicfoundation/hardhat-typechain": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^3.0.0",
    "@nomicfoundation/ignition-core": "^3.0.0",
    "@types/chai": "^5.0.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^22.0.0",
    "chai": "^5.1.2",
    "ethers": "^6.17.0",
    "hardhat": "^3.8.0",
    "mocha": "^11.0.0",
    "solhint": "^6.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}

# Only needed to deploy to or verify on a live network. The test suite is
# hermetic and needs none of these — `npm test` works with this file absent.
SEPOLIA_RPC_URL=
SEPOLIA_PRIVATE_KEY=
ETHERSCAN_API_KEY=

# Bancor IContractRegistry address for the network you are deploying to.
CONTRACT_REGISTRY=
